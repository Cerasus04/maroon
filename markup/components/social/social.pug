include ../ico-svg/ico-svg.pug

mixin social(params)
    ul.social-list&attributes(attributes)
        each item in params
            li.social-list__item
                a.social-list__link(href=item.href target="_blank" aria-label=item.ariaLabel)
                    +ico-svg(item.svg)
